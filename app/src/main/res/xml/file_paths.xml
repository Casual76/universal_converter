<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Converted files live in the cache until the user saves or shares them. -->
    <cache-path
        name="outputs"
        path="outputs/" />
    <!-- I file che un tool consegna a PampAI/Aria: temporanei, leggibili solo dal chiamante. -->
    <cache-path
        name="ai_bridge"
        path="ai-bridge/" />
</paths>
